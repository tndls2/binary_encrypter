version: '3'

services:
  client:
#    volumes:
#      - ${PWD}/client:/src
    image: tndls3026/binary_encrypter_client
    ports:
      - 3000:3000

  server:
    image: tndls3026/binary_encrypter_server
    ports:
      - 8080:8080
